# Applicant
A Template repository for a generic python project

## Usage
1. `pip install -r requirements.txt`
2. `python run.py` without arguments it'll create 2 files in current directory by the name of `cookies.json` storing session cookies & `job_listing.json` for scraped jobs.
