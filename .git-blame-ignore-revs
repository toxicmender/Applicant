# Commits that only reformatted code. `git blame` skips these when configured with:
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# The `format` workflow prints the SHA of each formatting commit it pushes; add it
# here so blame keeps pointing at whoever last changed the logic.
