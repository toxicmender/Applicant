[project]
name = "applicant"
version = "0.1.0"
description = "Scrapes applicable job postings, applies to them, and looks up company ratings"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
    "playwright>=1.44",
]

[tool.uv]
# an application, not a distributable package - nothing to build or install
package = false
